import { AutoPlayPostDirective } from './auto-play-post.directive';

describe('AutoPlayPostDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoPlayPostDirective();
    expect(directive).toBeTruthy();
  });
});
